You are the principal engineer responsible for delivering a production-quality local coding agent.

Your job is NOT to explain how to build it.

Your job is to ACTUALLY BUILD, TEST, DEBUG, VERIFY, AND PACKAGE the complete project.

PROJECT NAME:

m365-copilot-local-codex

============================================================
PRIMARY OBJECTIVE
============================================================

Build a local Codex-style coding agent for Windows that uses my EXISTING Microsoft 365 Copilot WEB access as its reasoning backend.

The final user experience should be:

1. I start the application.
2. A local web UI opens.
3. I select/open a local project workspace.
4. I type a coding task such as:

   "Fix the login timeout bug, update all affected files and tests, run the test suite, and stop only when the build passes."

5. The agent automatically:
   - inspects the workspace
   - searches relevant code
   - reads relevant files/ranges
   - asks M365 Copilot Web for reasoning
   - requests additional local context as needed
   - creates a plan
   - proposes multi-file edits
   - validates every edit
   - shows a unified diff
   - applies edits according to permission mode
   - runs build/tests
   - analyzes failures
   - performs corrective iterations
   - verifies the final build
   - shows a final change summary

The final experience should resemble a lightweight local version of:

Codex / Claude Code / Cline

but with:

Microsoft 365 Copilot Web

as the reasoning engine.

============================================================
ABSOLUTE REQUIREMENT: DO THE WORK
============================================================

Do not stop at:

- architecture
- explanation
- pseudocode
- scaffold
- TODOs
- partial implementation
- code snippets

Actually create all files.

Actually run the build.

Actually run tests.

Actually launch the application where execution is available.

Actually test the browser bridge where interactive login permits it.

Actually fix failures.

Actually create the ZIP.

Do not claim something works unless it was tested.

If a component cannot be tested because my manual Microsoft login/MFA is required, mark it explicitly:

NOT VERIFIED — USER AUTHENTICATION REQUIRED

Do not mark it PASS until it has actually succeeded.

============================================================
NO FALSE SUCCESS
============================================================

Never write:

"Done"
"Working"
"Fully functional"
"Production ready"

unless the corresponding verification succeeded.

Every major component must have one of:

PASS
FAIL
BLOCKED
NOT TESTED

Record these in:

BUILD_REPORT.md

============================================================
SECURITY / CORPORATE ENVIRONMENT
============================================================

This runs on a corporate Windows machine.

DO NOT:

- bypass company security controls
- bypass npm/Nexus restrictions
- change configured npm registry to npmjs
- use GitHub if GitHub is blocked
- bypass Microsoft MFA
- bypass Conditional Access
- extract authentication cookies
- copy browser cookies
- inspect Microsoft access tokens
- inspect refresh tokens
- replay Authorization headers
- scrape authentication storage
- disable TLS validation
- install MITM certificates
- intercept Microsoft HTTPS traffic
- reverse-engineer private Microsoft APIs
- call undocumented Copilot backend endpoints
- modify Group Policy
- disable endpoint protection
- attempt credential dumping
- use NODE_TLS_REJECT_UNAUTHORIZED=0
- silently download binaries from the internet

Use only normal browser UI interaction for M365 Copilot.

Authentication stays inside Microsoft Edge.

============================================================
HIGH LEVEL ARCHITECTURE
============================================================

Build:

┌───────────────────────────────────────┐
│ Local Web UI                          │
│ http://127.0.0.1:4140                 │
│                                       │
│ Workspace                             │
│ Chat / Task input                     │
│ Files                                 │
│ Agent progress                        │
│ Diff                                  │
│ Build/Test output                     │
│ Approve / Reject / Undo               │
└─────────────────┬─────────────────────┘
                  │
                  ▼
┌───────────────────────────────────────┐
│ Local Agent Runtime                   │
│ http://127.0.0.1:4142                 │
│                                       │
│ Workspace tools                       │
│ Agent orchestration                   │
│ Context management                    │
│ Patch validation                      │
│ Transaction manager                   │
│ Test/build loop                       │
│ Safety policy                         │
└─────────────────┬─────────────────────┘
                  │
                  ▼
┌───────────────────────────────────────┐
│ M365 Copilot Web Bridge               │
│ http://127.0.0.1:4141                 │
│                                       │
│ Browser automation                    │
│ Microsoft Edge                        │
│ Normal Copilot Web UI                 │
└───────────────────────────────────────┘

Everything must bind only to:

127.0.0.1

Never:

0.0.0.0

============================================================
PART 1 — STARTUP / ENVIRONMENT DETECTION
============================================================

At startup inspect:

node -v
npm -v
npm config get registry
npm config list

Detect:

- Windows version
- Node version
- npm version
- configured npm registry
- installed Microsoft Edge
- existing browser automation dependencies
- git availability
- PowerShell availability

Do NOT alter the configured registry.

Prefer built-in Node.js functionality where practical.

Use the minimum number of dependencies.

Before adding any dependency:

1. explain internally why it is needed
2. check whether it is available from the configured package source
3. prefer an already installed/approved alternative
4. if unavailable, record it in BLOCKERS.md

Do not bypass package controls.

============================================================
PART 2 — M365 COPILOT WEB BRIDGE
============================================================

The bridge must use the normal browser UI.

Flow:

local request
→ Edge
→ existing Microsoft 365 Copilot web UI
→ normal prompt submission
→ rendered response
→ local bridge response

Do not use private Copilot APIs.

============================================================
BROWSER AUTOMATION SELECTION
============================================================

Choose the first approved/available mechanism:

1. Playwright
2. Selenium/WebDriver
3. Edge WebDriver
4. Puppeteer
5. another existing approved browser automation mechanism

Prefer installed Microsoft Edge.

Do NOT automatically download Chromium.

Support:

BROWSER=edge

BROWSER_EXECUTABLE_PATH=

HEADLESS=false

Headed mode must be the default.

============================================================
AUTHENTICATION
============================================================

First launch:

1. open normal Edge
2. navigate to configured Copilot page
3. allow me to log in normally
4. allow normal corporate SSO
5. allow Microsoft Authenticator/MFA
6. wait for Copilot ready state

Never request my password in the application.

Never extract authentication material.

Use browser-managed profile persistence only if allowed normally by Edge.

If the session expires:

show login page again.

============================================================
COPILOT CONFIG
============================================================

Environment variables:

COPILOT_URL=
COPILOT_MODE=auto
COPILOT_TIMEOUT_MS=180000
COPILOT_RESPONSE_STABILITY_MS=1500

Modes:

auto
default
think-deeper

If Think Deeper exists as a visible supported UI option:

select it through the normal UI.

If not available:

report the limitation.

Never invoke a hidden/private model endpoint.

============================================================
ROBUST SELECTORS
============================================================

Create:

src/bridge/copilot/selectors.ts

Selectors must prioritize:

1. ARIA role
2. accessible label
3. stable semantic attributes
4. placeholder/text labels
5. stable data attributes

Avoid generated CSS classes.

Each important UI control must have fallback selectors.

Important targets:

- prompt input
- send button
- stop-generating button
- new chat button
- reasoning mode controls
- assistant response container

============================================================
COPILOT STATE MACHINE
============================================================

Implement explicit states:

STARTING
LOGIN_REQUIRED
MFA_REQUIRED
READY
GENERATING
IDLE
SESSION_EXPIRED
RATE_LIMITED
ACCESS_DENIED
UI_CHANGED
UNAVAILABLE
ERROR

Do not infer READY simply because the page loaded.

============================================================
RESPONSE COMPLETION
============================================================

Never depend only on fixed sleep().

Use multiple completion signals:

- generation UI state
- stop button disappearance
- send button state
- response DOM mutation
- response stability timer

A response is complete only after:

generation appears finished
AND
response content is stable for configured time

============================================================
RESPONSE EXTRACTION
============================================================

Extract only the latest assistant response.

Preserve:

- Markdown
- headings
- paragraphs
- bullets
- numbered lists
- code blocks
- inline code

Exclude:

- sidebar
- previous messages
- user's own prompt
- navigation text
- button labels
- unrelated citation UI

============================================================
BRIDGE API
============================================================

Bridge:

127.0.0.1:4141

Implement:

GET /health
GET /status
GET /v1/models
POST /v1/chat/completions
POST /control/new-chat

Model:

m365-copilot-web

Aliases:

m365
copilot
m365-copilot
m365-copilot-web

============================================================
BRIDGE QUEUE
============================================================

Browser generation concurrency must default to:

1

Implement FIFO queue.

Never allow two tasks to type into the same browser conversation simultaneously.

============================================================
PART 3 — LOCAL CODING AGENT
============================================================

Agent server:

127.0.0.1:4142

This must be a REAL iterative coding agent.

Not a one-shot prompt wrapper.

============================================================
REQUIRED AGENT TOOLS
============================================================

Implement:

workspace_info

list_files

find_files

search_text

read_file

read_file_range

read_multiple_files

file_exists

git_status

git_diff

git_diff_file

get_diagnostics

propose_edit

propose_patch

create_file

rename_file

delete_file

run_build

run_tests

run_typecheck

run_lint

run_command

rollback_changes

============================================================
WORKSPACE SANDBOX
============================================================

Every filesystem operation must remain inside the selected workspace.

Before every operation:

1. resolve absolute path
2. normalize Windows path
3. resolve symlinks/junctions where possible
4. confirm final path remains inside workspace root

Reject:

../

absolute external paths

UNC escapes

junction escapes

symlink escapes

case-insensitive path tricks

alternate drive escapes

Never modify a file outside the selected workspace.

============================================================
DEFAULT EXCLUSIONS
============================================================

Do not automatically inspect/send:

.git/
node_modules/
dist/
build/
coverage/
target/
.env
.env.*
*.pem
*.key
*.pfx
*.p12
id_rsa*
credentials
browser profiles
cloud credential directories

Respect:

.gitignore

Also support:

.m365agentignore

============================================================
SECRET REDACTION
============================================================

Before code/context is sent to M365 Copilot:

run secret detection.

Detect common:

Bearer tokens
JWTs
AWS access keys
API keys
private keys
password assignments
connection strings containing credentials
client secrets

Default:

REDACT_SECRETS=true

Never log secret values.

============================================================
AGENT TOOL PROTOCOL
============================================================

Because Copilot Web may not provide native function calling:

implement a strict structured JSON tool protocol.

Valid Copilot action:

{
  "type": "tool_call",
  "id": "call_001",
  "tool": "search_text",
  "arguments": {
    "query": "CampaignForm",
    "path": "src"
  }
}

Local response:

{
  "type": "tool_result",
  "id": "call_001",
  "ok": true,
  "result": []
}

Never execute freeform prose as a tool call.

============================================================
JSON PARSER
============================================================

Handle:

raw JSON

```json fenced JSON

valid balanced JSON embedded in surrounding text

But:

if multiple conflicting actions exist:

reject and ask Copilot to resend one valid action.

Validate every call using explicit schemas.

============================================================
AGENT LOOP
============================================================

Implement:

USER TASK
↓
create dedicated Copilot conversation
↓
send agent protocol + task
↓
Copilot requests context/tool
↓
validate request
↓
execute safe local tool
↓
send observation
↓
repeat
↓
Copilot proposes edits
↓
validate ALL edits
↓
generate full multi-file diff
↓
review/approval according to mode
↓
transactionally apply
↓
build/test
↓
if failure:
    send relevant sanitized error context
    continue agent loop
↓
verify
↓
final report

Config:

MAX_AGENT_STEPS=40
MAX_FIX_ITERATIONS=5

Stop cleanly when limits are reached.

Never infinite-loop.

============================================================
AGENT MODES
============================================================

Implement:

ask
plan
review
agent

ASK:
read-only questions about code

PLAN:
analyze workspace and return implementation plan
no file modifications

REVIEW:
inspect current git/uncommitted changes
no automatic modifications

AGENT:
read/search/edit/build/test loop

============================================================
PERMISSION MODES
============================================================

AGENT_PERMISSION_MODE=review

Modes:

review
auto-edit

Default:

review

REVIEW:

reading/search automatic

editing requires user approval

commands beyond known-safe build/test require approval

AUTO-EDIT:

workspace edits permitted automatically

safe build/test commands automatic

dangerous operations still blocked

Never automatically switch modes.

============================================================
MULTI-FILE EDIT TRANSACTIONS
============================================================

This is critical.

If Copilot proposes changes to:

A.ts
B.ts
C.ts

before changing anything:

validate ALL three.

Confirm:

- all paths valid
- all anchors exist
- all expected text matches
- all files writable
- none outside workspace

Generate complete diff.

Only then apply.

If ANY edit fails:

ROLL BACK ALL changes from the transaction.

Never leave half-applied multi-file edits.

============================================================
PRE-EDIT SNAPSHOT
============================================================

Before every edit transaction:

capture original content for every touched file.

Store only in a temporary local transaction store.

Support:

Undo Last Change

Rollback Current Task

Do not depend solely on Git.

============================================================
PATCH STRATEGY
============================================================

Prefer minimal targeted changes.

Do not rewrite entire files unless necessary.

Preferred operations:

exact replacement

line range replacement

create file

delete file

rename file

Each exact replacement must satisfy:

old text exists

old text uniquely matches

If not:

reject edit

ask Copilot to re-read the relevant section

Do NOT "best guess" the match.

============================================================
OPTIMISTIC CONCURRENCY CHECK
============================================================

Before writing:

confirm the file has not changed since it was read.

Use content hash.

If hash changed:

do not overwrite.

Mark:

FILE_CHANGED_EXTERNALLY

Re-read and ask Copilot to reconsider.

============================================================
ATOMIC WRITES
============================================================

Where possible:

write to temporary file

fsync/close

rename atomically

Do not write directly over important files if a safer atomic method is available.

============================================================
LINE ENDINGS / ENCODING
============================================================

Preserve:

CRLF/LF

UTF-8 BOM where present

file encoding where practical

Do not unnecessarily reformat entire files.

============================================================
COMMAND EXECUTION
============================================================

Use executable + argument arrays.

Prefer:

spawn
execFile

Avoid shell:true.

Never execute Copilot prose as shell.

============================================================
SAFE COMMAND DETECTION
============================================================

Detect project ecosystem.

Node:

npm run build
npm test
npm run test
npm run typecheck
npm run lint

Java:

mvn test
mvn package

gradlew.bat test
gradlew.bat build

Python:

pytest
python -m pytest

Run existing repository scripts where possible.

Do not invent random commands.

============================================================
BLOCKED COMMANDS
============================================================

Block dangerous operations, including equivalent variants:

rm -rf
rmdir /s
del /s
format
diskpart
shutdown
reboot
git reset --hard
git clean -fd
git clean -fdx
git checkout -- .
git restore .
Remove-Item -Recurse on workspace root
registry edits
Group Policy changes
credential access
browser authentication storage access
TLS disabling
PowerShell download-and-execute
curl piped to shell
arbitrary executable downloaded from network

============================================================
NETWORK
============================================================

Default:

ALLOW_AGENT_NETWORK_COMMANDS=false

Agent must not arbitrarily upload repository content.

Do not upload code to services other than the configured M365 Copilot Web interaction.

============================================================
BUILD / TEST REPAIR LOOP
============================================================

After changes:

1. determine project type
2. run typecheck if applicable
3. run relevant tests
4. run build

Capture:

exit code
stdout
stderr
duration

If failure:

send only relevant diagnostics to Copilot.

Include:

file names
line numbers
error messages
small relevant context

Do not send massive logs.

Then allow targeted repair.

MAX_FIX_ITERATIONS=5.

============================================================
DEFINITION OF SUCCESS
============================================================

A coding task is NOT successful merely because edits were applied.

Success requires:

- all requested edits applied
- edit transaction valid
- no unresolved patch failures
- selected verification command succeeded
- final diff available
- no known safety violation

If tests cannot run:

report:

COMPLETED_WITHOUT_TEST_VERIFICATION

not:

SUCCESS

============================================================
PART 4 — LOCAL WEB UI
============================================================

Build:

http://127.0.0.1:4140

Prefer a lightweight implementation.

Avoid a heavy frontend dependency if unnecessary.

UI layout:

LEFT:
workspace/files

CENTER:
agent conversation/progress

RIGHT:
diff/review

BOTTOM:
build/test/terminal output

============================================================
WEB UI TOP BAR
============================================================

Show:

Workspace

Agent mode:
Ask / Plan / Review / Agent

Permission:
Review / Auto-edit

Copilot status

Browser status

Build status

============================================================
TASK INPUT
============================================================

Large input:

"Ask agent..."

Buttons:

Run

Stop

New Task

Review Changes

Approve

Reject

Undo

Run Tests

Run Build

============================================================
AGENT PROGRESS
============================================================

Show live events such as:

Analyzing workspace

Searching "CampaignForm"

Reading src/CampaignForm.tsx

Reading src/hooks/useValidation.ts

Asking M365 Copilot

Copilot requested additional context

Planning 3 file edits

Validating patch

Awaiting approval

Applying transaction

Running npm test

2 tests failed

Diagnosing

Applying repair

Running tests

42 tests passed

Running build

Build passed

Completed

============================================================
FILE TREE
============================================================

Show workspace tree.

Do not display ignored directories by default.

Clicking a file should show its content read-only unless opened for edit review.

============================================================
DIFF VIEW
============================================================

Support:

multi-file unified diff

before/after side-by-side view where practical

Per file:

Accept
Reject

Also:

Accept All
Reject All

But if per-file acceptance would invalidate the transaction:

warn the user.

============================================================
UNDO
============================================================

Provide:

Undo Last Applied Transaction

Restore exact previous file contents.

============================================================
WEBSOCKET/SSE
============================================================

Use SSE or WebSocket for agent progress.

Prefer SSE if simpler and sufficient.

============================================================
AGENT HTTP API
============================================================

Implement approximately:

GET /health

GET /status

POST /workspaces/open

GET /workspace/files

GET /workspace/file

POST /tasks

GET /tasks/:id

GET /tasks/:id/events

GET /tasks/:id/diff

POST /tasks/:id/approve

POST /tasks/:id/reject

POST /tasks/:id/cancel

POST /tasks/:id/undo

POST /tasks/:id/run-tests

POST /tasks/:id/run-build

============================================================
PART 5 — OPTIONAL VS CODE INTEGRATION
============================================================

Build a minimal VS Code extension if dependencies/API access permit it.

It must NOT be required for core functionality.

The browser UI must work independently.

Commands:

M365 Agent: Open UI

M365 Agent: Start Task

M365 Agent: Review Changes

M365 Agent: Run Tests

M365 Agent: Show Status

M365 Agent: Undo Last Change

Use the existing local agent server.

Do not duplicate the agent engine inside the extension.

============================================================
WORKSPACE TRUST
============================================================

If integrated with VS Code:

respect Workspace Trust.

Untrusted workspace:

read-only analysis only

no file writes

no command execution

============================================================
PART 6 — CONTEXT MANAGEMENT
============================================================

Do not send the entire repository to Copilot.

Use retrieval.

Typical process:

workspace overview

search symbols/text

read likely entry points

read related types/interfaces

read tests

read only relevant ranges

Track already-read content.

Avoid duplicate context.

Config:

MAX_CONTEXT_CHARS=200000
MAX_FILE_BYTES=500000

If file is too large:

search first

read ranges only.

============================================================
INTERNAL CODING AGENT PROMPT
============================================================

Create a strong system prompt internally for the M365 Copilot reasoning engine.

It must say approximately:

You are the reasoning engine of a local coding agent.

You do not directly access the filesystem.

Never assume file contents.

Request context through provided tools.

Search before assuming.

Read relevant implementation and tests before editing.

Prefer minimal changes.

Preserve existing architecture and style.

Do not modify unrelated code.

Do not fabricate successful build/test results.

Use only the structured action protocol.

When editing, request precise modifications.

If an edit fails validation, re-read the file.

If tests fail, diagnose from actual error output.

Do not ask the human to manually edit files when supported tools can perform the operation.

Return final only when verification is complete or a real blocker is identified.

============================================================
PART 7 — SELF VALIDATION / TEST SUITE
============================================================

Create automated tests for all critical components.

Tests MUST include:

PATH SECURITY

- normal workspace path allowed
- ../ escape rejected
- absolute outside path rejected
- symlink/junction escape rejected where testable

EDIT ENGINE

- exact unique replacement works
- missing oldText rejected
- duplicate oldText rejected
- multi-file transaction succeeds
- transaction failure rolls everything back
- external file modification detected
- undo restores exact bytes

JSON TOOL PARSER

- direct JSON
- fenced JSON
- JSON surrounded by prose
- invalid JSON rejected
- unknown tool rejected
- invalid args rejected
- multiple ambiguous actions rejected

COMMAND POLICY

- npm test allowed
- npm run build allowed
- git status allowed
- git diff allowed
- git reset --hard blocked
- rm -rf blocked
- PowerShell recursive deletion blocked
- download-and-execute blocked

QUEUE

- only one Copilot generation runs at a time
- queued tasks preserve order

HTTP

- health endpoint
- malformed JSON
- request size limit
- localhost binding

TRANSACTION

- three-file change applies atomically
- simulated second-file write failure causes full rollback

============================================================
PART 8 — REAL SMOKE TEST
============================================================

Perform local smoke tests.

1:

GET local UI health

2:

GET bridge health

3:

GET agent health

4:

Open a TEMPORARY test workspace created specifically for testing.

Never use my real repo for destructive smoke tests.

Create sample files.

Ask agent to:

"Rename add() to sum() and update all references and tests."

Verify:

- search
- read
- multi-file edit
- diff
- approval simulation
- apply
- test
- undo

Verify original files restored after undo.

============================================================
REAL COPILOT TEST
============================================================

When Microsoft login is required:

pause ONLY for:

"Please complete Microsoft login/MFA in the browser."

After I complete login:

continue.

Test prompt:

Reply with exactly:

M365_AGENT_BRIDGE_OK

Do not treat login page accessibility as successful Copilot communication.

The exact response must actually be received.

============================================================
END-TO-END AGENT TEST
============================================================

Using temporary sample project:

ask through the complete agent pipeline:

"Change the greeting from Hello to Hello World and update the test."

The system must:

- query M365 Copilot
- request/read files
- create edit
- apply transaction
- run test
- report result

If this cannot be verified because Copilot login is unavailable:

mark it BLOCKED, not PASS.

============================================================
PART 9 — ERROR RECOVERY
============================================================

Implement clean recovery for:

browser crash

Copilot page reload

session expiration

UI selector change

request timeout

invalid Copilot JSON

edit validation failure

test timeout

command failure

user cancellation

agent step limit

Copilot refusal

If browser dies:

restart browser once.

If login is required:

wait for human.

Never automatically bypass authentication.

============================================================
PART 10 — LOGGING
============================================================

Environment:

LOG_LEVEL=info
LOG_CONTENT=false

Never log:

passwords

cookies

tokens

authorization headers

MFA data

secret values

entire source files by default

Logs may include:

file path
tool name
duration
exit status
agent state

============================================================
PART 11 — CONFIG
============================================================

Create:

.env.example

Include:

UI_HOST=127.0.0.1
UI_PORT=4140

BRIDGE_HOST=127.0.0.1
BRIDGE_PORT=4141

AGENT_HOST=127.0.0.1
AGENT_PORT=4142

COPILOT_URL=
COPILOT_MODE=auto
COPILOT_TIMEOUT_MS=180000
COPILOT_RESPONSE_STABILITY_MS=1500

BROWSER=edge
BROWSER_EXECUTABLE_PATH=
HEADLESS=false

AGENT_MODE=agent
AGENT_PERMISSION_MODE=review

MAX_AGENT_STEPS=40
MAX_FIX_ITERATIONS=5

MAX_CONTEXT_CHARS=200000
MAX_FILE_BYTES=500000
MAX_QUEUE_SIZE=10

REDACT_SECRETS=true
ALLOW_AGENT_NETWORK_COMMANDS=false

LOG_LEVEL=info
LOG_CONTENT=false

DIAGNOSTIC_SCREENSHOTS=false

============================================================
PART 12 — PROJECT STRUCTURE
============================================================

Use a clean structure similar to:

m365-copilot-local-codex/

package.json
package-lock.json
tsconfig.json
.env.example
.gitignore
README.md
BUILD_REPORT.md
BLOCKERS.md
SECURITY.md

src/

  index.ts
  config.ts

  ui/
    server.ts
    public/
      index.html
      app.js
      styles.css

  bridge/
    server.ts
    queue.ts
    openaiAdapter.ts

    copilot/
      browser.ts
      selectors.ts
      stateDetector.ts
      responseExtractor.ts
      copilotPage.ts

  agent/
    server.ts
    orchestrator.ts
    taskManager.ts
    contextManager.ts
    toolProtocol.ts
    toolParser.ts
    permissions.ts

    tools/
      workspace.ts
      files.ts
      search.ts
      git.ts
      edit.ts
      commands.ts
      diagnostics.ts

    edits/
      patchEngine.ts
      transaction.ts
      diff.ts
      undo.ts

    security/
      paths.ts
      secrets.ts
      commandPolicy.ts

    prompts/
      codingAgent.ts

tests/

scripts/

vscode-extension/
  if practical

You may improve the structure.

============================================================
PART 13 — NPM SCRIPTS
============================================================

Implement:

npm run dev

npm run build

npm start

npm test

npm run typecheck

npm run smoke

npm run test:bridge

npm run test:agent

npm run test:security

npm run test:transaction

npm run package

============================================================
PART 14 — README
============================================================

README must include exact Windows instructions.

First run:

npm install
npm run build
npm start

Explain:

1. startup
2. browser login
3. workspace selection
4. Ask mode
5. Plan mode
6. Agent mode
7. Review mode
8. reviewing diff
9. approving changes
10. undo
11. running tests
12. troubleshooting Copilot UI changes
13. corporate Nexus limitations
14. safety model

============================================================
PART 15 — BUILD GATES
============================================================

Do not package until these succeed if technically possible:

GATE 1
TypeScript typecheck

GATE 2
unit tests

GATE 3
security tests

GATE 4
transaction rollback tests

GATE 5
HTTP smoke tests

GATE 6
local UI launch

GATE 7
browser launch

GATE 8
Copilot real-response test
if authentication is available

GATE 9
end-to-end temporary workspace coding task
if Copilot communication is available

If any mandatory local gate fails:

FIX IT BEFORE PACKAGING.

If Copilot-specific gate cannot run only because manual account access is unavailable:

package project but mark:

BLOCKED_PENDING_USER_LOGIN

============================================================
PART 16 — CODE REVIEW YOUR OWN WORK
============================================================

Before final packaging:

perform a complete self-review.

Search for:

TODO

FIXME

throw new Error("Not implemented")

mock-only production paths

hardcoded passwords

hardcoded tokens

0.0.0.0

TLS disabling

unsafe shell execution

path traversal

missing error handling

unbounded loops

unbounded request bodies

missing timeouts

unawaited promises

swallowed exceptions

partial transaction writes

incorrect Windows path handling

Fix issues found.

============================================================
PART 17 — FINAL CLEAN BUILD
============================================================

Delete generated build output where appropriate.

Run clean install/build using the permitted package source if feasible.

Run:

npm run typecheck
npm test
npm run build
npm run smoke

Capture exact results.

============================================================
PART 18 — BUILD REPORT
============================================================

BUILD_REPORT.md must include:

System information

Node version

npm version

npm registry

Browser automation mechanism

Browser executable

Dependencies

Typecheck result

Unit test result

Security test result

Transaction test result

UI smoke result

Bridge result

Copilot login result

Real Copilot prompt result

Agent E2E result

Known limitations

Blocked items

Every line must use:

PASS
FAIL
BLOCKED
NOT TESTED

No vague wording.

============================================================
PART 19 — PACKAGE
============================================================

Create:

m365-copilot-local-codex.zip

Exclude:

node_modules/

.env

browser profiles

cookies

tokens

authentication data

logs containing source code

diagnostic screenshots

temporary transaction backups

temporary test workspaces

Include:

source code

package files

tests

README

SECURITY.md

BUILD_REPORT.md

BLOCKERS.md

VS Code extension source if built

============================================================
ZIP VERIFICATION
============================================================

After creating the ZIP:

1. verify it exists
2. list archive contents
3. ensure required files exist
4. ensure .env is NOT present
5. ensure browser profile is NOT present
6. ensure node_modules is NOT present
7. ensure no token/cache file is present
8. extract ZIP into a fresh temporary directory
9. verify project structure
10. run a basic build from the extracted copy where feasible

Do not declare final delivery before this verification.

============================================================
FINAL RESPONSE
============================================================

At the end give me ONLY:

ZIP:
<absolute path>

BUILD:
PASS / FAIL

LOCAL TESTS:
PASS / FAIL

COPILOT BRIDGE:
PASS / BLOCKED / FAIL

END-TO-END AGENT:
PASS / BLOCKED / FAIL

BLOCKERS:
<short list or NONE>

START:
<exact Windows commands>

URL:
http://127.0.0.1:4140

Do not paste source code into chat.

Do not provide a tutorial instead of the ZIP.

Start building now.
